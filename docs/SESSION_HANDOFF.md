# Калинино ЦКС — Session Handoff

> Sticky-note для непрерывности сессий. Перезаписывается `/close_session`. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ⏳ ПЕРЕДАЧА В ДК МАЛМЫЖ (D-074) — сайт живёт до строки ДК «раздел живёт», ничего не выключено
**Updated:** 2026-09-04
**Branch:** main

## Текущая нитка

**Решение владельца 03.09 (D-074):** проект закрывается, наработки и данные переходят в культура.вмалмыже.рф (ДК Малмыж) как раздел «Калининский СДК». Мандаты 03.09 (паспорт в vault, аудиты D-038/vars/G286) сняты тем же письмом Мозга.

**Сделано 04.09 (эта сессия):**
- Выгрузка для передачи снята на Боксе 1: `/home/valstan/kalinino/handover-2026-09-04/` (599 МБ, пользователь `valstan`, общий с ДК). Дамп БД (custom, без транзитных таблиц Payload), схема SQL, `posts.json`/`categories.json`/`media.json`, `media.tar.gz` (945 файлов) с манифестом путь→sha256→vkPostId, `manual-edits.json`, `README.md`. Секреты проверены грепом с непустым контролем — не найдены.
- **Ручных правок текста после импорта — 0** (по `_posts_v`). Расхождения с импортом только: публикация (17 постов), рубрика от переразбора 22.08 (11), обложка от переимпорта (15). 131 медиа-файл — сироты переимпорта 22.08, дубликаты по sha256, помечены в манифесте.
- На проде сейчас 48 постов: 46 опубликовано, 2 черновика (импорт 30.08). Владелец опубликовал все августовские черновики сам.
- `docs/VISUAL_IDENTITY.md` — визуальный образ для переноса (токены, шрифты, приёмы, ассеты).
- D-066 исполнен: `obriv.md` удалён, ссылка в `AGENTS.md` заменена; косметика из аудита Fable (дата парковки домена) снята.
- Письмо Мозгу `mailbox/to-brain/2026-09-04-d074-handover-ready-and-answers.md`: приёмка D-074 + ответы на список «без ответа» 03.09 (D-038 был отвечен 28.08 и принят 29.08).

**Открыто:** ждём план ДК и строку «раздел живёт, редирект стоит, контент сверен» + подтверждение Мозга. До этого **ничего не выключать**: юнит, таймер `kalinino-vk-sync`, домен, сертификат.

## Сделано (сессия 2026-08-22)

- **PR-гейт `gates`** (PR #19): `.github/workflows/ci.yml` на `pull_request` — gitleaks по всей истории → lint → typecheck → build против эфемерного Postgres 17. Фильтров по путям нет намеренно (обязательная `paths:`-проверка подвесила бы PR, не трогающий эти пути). Образец — `ci.yml` ДК, шаг секретов — Казанской. Красный прогон `32573733311`, зелёный `32573835157`. Имя job'а ушло Мозгу, он делает его обязательным на своей стороне.
- **Сканер нашёл реальное:** 65 срабатываний в `scripts/.work/wall.json` — сырой дамп VK API (1 МБ), закоммиченный в PR #12 и с 17.08 лежавший в публичном репо (`access_key`/`web_view_token` медиа публичной стены, не наши ключи). Каталог снят с учёта и в `.gitignore`; в истории остаётся — переписывать нельзя, поэтому `.gitleaks.toml` с задокументированным исключением пути.
- **Метрика + информер (D-017, PR #21):** счётчик заведён самим в кабинете владельца — **№ 111854806** «СДК Калинино», домен внесён **только кириллицей** (амендмент G136 от 10.08: punycode Метрика отвергает — мандат 09.08 и напоминание 22.08 повторяли устаревший совет, Мозгу доложено). «Принимать данные только с указанных адресов» — вкл, Вебвизор — выкл. На сайте: `/api/analytics-config` (номер с рантайма, не запекается в CI-бандл), `/api/analytics-informer` (прокси бейджа через свой origin — G80), `/api/analytics-stats`, `Analytics.client` (отложенная загрузка, hit на SPA-навигации), `/privacy`. `YM_COUNTER_ID=111854806` в `/etc/kalinino/kalinino.env` (бэкап `.bak-20260822`).
- **Приёмка по G237 — глазами:** бейдж на проде показывает **1/1/1**, цифру посчитал Яндекс. «200 image/png» за приёмку не считалось.
- **Читаемость подвала (PR #22):** живой рендер вскрыл, что текст подвала тонул в узоре гирлянды (копирайт и подпись — ещё до Метрики). Текстовая колонка получила тёмную плашку `site-footer__col`.
- **Кнопка «Сервисы Малмыжа» (PR #20)** в шапке, punycode-хостом (G133/G134), образец — `EthnoDrawer` Гоньбы. Четыре поля карточки каталога ушли Мозгу: «Сельский дом культуры Калинино», 🪗, `https://сдк-калинино.вмалмыже.рф`, описание без дат.
- **Рефлекс #014 + keep-alive (PR #23):** в `AGENTS.md` раздел «📚 Библиотека Мозга» (греп `GOTCHAS.md` по симптому до расследования) и описание PR-гейта. G234 проверен на боксе: `KEEP_ALIVE_TIMEOUT` не был задан → **65000** в прод-env, сервис перезапущен, `/` = 200. Полезное для портфеля: **Next standalone `server.js` читает именно эту env-переменную**, флага запуска нет.
- **Отчёт Мозгу (PR #24)** — построчный статус всех пунктов, находка про дампы выгрузок как кандидат в библиотеку, поправка по punycode.

## Сделано (продолжение сессии 2026-08-22)

- **Graceful shutdown (PR #27).** Разобрались с последним открытым пунктом Мозга и нашли, что **наше же утреннее письмо было неверно**: обработчик SIGTERM у Next есть (`start-server.js:356-359`). Настоящая грабля тоньше: `closeAllConnections()` стоит под `if (isDev)`, поэтому сокет, дописавший ответ уже ПОСЛЕ начала остановки, держит `server.close()`. Замер на боксе (Node 20): **29 089 мс без обёртки против 2149 мс с ней**, тело ответа доезжает в обоих случаях. Значит выставленный утром `KEEP_ALIVE_TIMEOUT=65000` **сам по себе удлинял простой**. Обёртка `deploy/server-graceful.mjs` (перехват `http.createServer`, FIN по завершении ответа, форс-выход, `headersTimeout`), `ExecStart` переключён, `TimeoutStopSec=30`. На проде рестарт при висящем keep-alive — **57 мс**. Поправка Мозгу отправлена (PR #28): выход не 143, а 0; «обёртка ради нуля» верно только для idle-сокета.
- **🔴 Живой VK-токен в публичном репозитории Гоньбы** (PR #26). Найден при чтении соседей как образцов: `Valstan/Gonba/seed-vk-source.sql`, файл в индексе git, отдаётся GitHub API, репозиторий публичный с 17.08. Владельцу сообщено, письмо Мозгу отправлено: отозвать токен → выпустить новый в env/КАРМАН → убрать файл → раскатать сканер на все репозитории. **Значение токена нигде не приводилось.** Чужой репозиторий мы не правили.
- **Автоимпорт из ВК (PR #29, #30, #31).** Решение владельца: не ждать конвейер Сарафана (наше сообщество у них не заведено в мониторинг, звено 5 без срока, конвейер снят с учёта Мозга 17.08). Заведён **временный мотор**: `lib/vk-sync.ts` (отбор, очистка, рубрикатор, заголовок) + `api/vk-sync/run` (шлюз Сарафана → отсев уже импортированных по `vkPostId` → собственный ingest) + systemd-таймер раз в сутки. Новых секретов не потребовалось. Рубрикатор проверяется тестом на 29 постах с ручной разметкой владельца — **27/29**; попутно исправлен свой баг: `\w` в JS не включает кириллицу, из-за чего морфологические правила молча не срабатывали.
- **Сухой прогон окупился сразу:** первый `?dry=1` показал, что мотор потянул бы пять записей подряд без единого слова текста. Владелец решил, что афиши нужны — они едут с заголовком «Афиша от 11 июня 2026 г.».
- **На проде:** 15 постов импортированы **черновиками** (публикация выключена), ошибок ноль, медиа перелились. Таймер `kalinino-vk-sync.timer` включён, следующий прогон 23.08 в 06:52 MSK.
- **Тесты появились в проекте:** vitest, 15 тестов, шаг `Test` добавлен в job `gates`.
- Долг «почистить старый `NEXT_PUBLIC_SERVER_URL`» **закрыт как неактуальный**: мёртвого домена нет ни в env, ни в nginx, ни в коде.

## Сделано (ночная часть, 2026-08-23)

- **Рубрика «Афиша» (PR #34)** — решение владельца: анонс и отчёт о прошедшем врозь нагляднее. Пост без единого слова текста → `afisha`; анонс по тексту («приглашаем», «состоится», «начало в 18:00») тоже афиша, и это правило проверяется первым. Отчёт о прошедшем концерте остаётся в «Концертах» — закреплено тестом. Точность на эталонной разметке не изменилась: 27/29.
- **Режим `?refresh=1` (PR #35)** — переобновить уже импортированные черновики, когда меняются правила разбора. Таймер его не использует, только руками.
- **🔴 Найдена и закрыта дыра в схеме (PR #36).** Переимпорт черновиков падал 500 ровно на том посте, который реально обновлялся: в `payload_locked_documents_rels` **не было колонки `categories_id`** — августовская рукописная миграция завела `Categories`, но служебную таблицу блокировок не тронула. **Создание документа её не читает, обновление читает** — поэтому весь августовский импорт был зелёным, а мина ждала 18 дней. Миграция `20260822_214500` применена штатно через `apply-migration.yml` → `payload_migrations` → деплой через dispatch (#017). После этого 30 обновлений подряд без ошибок. Дополнение к нашему же чеклисту G231 отправлено Мозгу (PR #38).
- **Пустые рубрики больше не показываются (PR #37)** — автоимпорт заводит рубрику, когда первый пост в ней ещё черновик, и на сайте появлялся чип, ведущий в пустоту. Теперь чипы строятся только по рубрикам с опубликованными постами.
- **На проде:** 15 черновиков, из них 11 афиш уже в рубрике «Афиша» (проверено по актуальным версиям), «Афиша» поставлена первой в порядке рубрик. Опубликованных постов по-прежнему 29.

## Сделано (сессия 2026-08-28)

- **Аудит D-038 по содержимому.** Семь параллельных проходов по категориям мандата + проход «что свип пропустил» по всем отслеживаемым файлам: 318 сырых вхождений → 217 уникальных → **58 реальных** в 22 файлах (159 признаны безобидными: дев-локалхост, версии пакетов, публичный домен витрины, номер счётчика). Разбивка: 8 в живых документах, 38 в функциональных файлах, 12 в исторических.
- **Вычищено 46** (живые + функциональные), исторические 12 **не тронуты** — прямой запрет мандата.
- **Самое серьёзное — не IP, а FQDN шлюза Сарафана**, захардкоженный в двух скриптах и в живом роуте автоимпорта: это адрес **чужого** бокса с ключевой аутентификацией. Переехал в `SARAFAN_GATEWAY_URL` в env-файле сервиса (заведён на проде до правки кода, чтобы не уронить единственный работающий конвейер), в коде — чтение env и 503 при отсутствии.
- **Предохранитель переписан через свойство** — `AGENTS.md` §🛡 «Публичный репозиторий — recon-поверхность»: *всё, что попало в отслеживаемый файл, считается опубликованным*; список категорий; куда класть вместо репо; требование грепа; честная граница про необратимость; запрет править историю.
- **Правило D-046** — `AGENTS.md` §🧰 «Текст — файлом», с оговоркой, что оно про сочинение текста агентом, а не про heredoc'и в уже коммитнутых workflow (иначе следующий агент пошёл бы «чинить» рабочий деплой).
- **Найдена вторая recon-поверхность, которой нет в формулировке мандата: логи прогонов Actions публичны.** Три канала, все были у нас: значение в `vars` (см. ниже); `echo "host: $(hostname)"` в preflight миграций → удалено; дамп `systemctl status` / `journalctl` при падении → `-o cat` и `-n 0` плюс редактирование вывода (строки подключения и домашний каталог). Отправлено Мозгу как кандидат в библиотеку — с оговоркой, что рассылать стоит отдельно от D-038: у любого публичного репозитория портфеля реквизиты из `vars` уже лежат в логах прошлых прогонов.
- **🔴 Две собственные починки оказались неверными; поймала их состязательная вычитка диффа перед отправкой.**
  - **`env:` не защищает.** Вынести значение из `${{ }}` в блок `env:` шага мало: раннер печатает **сам блок `env:` с раскрытыми значениями**, маскируя только `secrets`. Проверено на нашем логе (деплой 22.08, шаг Setup SSH): `SSH_PRIVATE_KEY: ***`, а рядом хостнейм и SSH-порт открытым текстом. → `DEPLOY_SSH_HOST`, `DEPLOY_SSH_USER`, `APP_PORT`, `PROD_ENV_FILE` переведены в **`secrets`**, одноимённые `vars` удалены. `DEPLOY_SSH_PORT` осознанно оставлен в `vars`: 22 универсален, а маскировка такой короткой строки замазала бы все даты в логе.
  - **Редактор журнала не ловил хостнейм.** `journalctl` по умолчанию печатает «дата ХОСТНЕЙМ юнит[pid]: текст» — имя машины в каждой из 80 строк; мой `sed` его не трогал. Проверял фильтр на том, что собирался фильтровать, а не на реальном формате вывода. → `-o cat` у `journalctl`, `-n 0` у `systemctl status`, `sed` остался вторым рубежом.
  - Обе ошибки одного вида: **проверка починки на модели вместо артефакта.**
- **Реквизиты в `secrets`:** `DEPLOY_SSH_HOST`, `DEPLOY_SSH_USER`, `APP_PORT`, `PROD_ENV_FILE`. В `vars` осталось только публичное: `NEXT_PUBLIC_SERVER_URL` (адрес витрины) и `DEPLOY_SSH_PORT`. Корень деплоя — `$HOME/kalinino`, раскрывается на самой машине; чтобы развязка не стала тихой, деплой перед рестартом сверяет `WorkingDirectory` юнита с разложенным релизом и падает при расхождении (иначе поднялся бы старый код, а smoke-check покрасил бы прогон зелёным). Дев-дефолты порта переведены на нейтральный 3000.
- **Приёмка показана артефактом, а не рассуждением** (PR #40 влит, деплой `33122959303` зелёный):
  - в новом публичном логе прогона `DEPLOY_SSH_HOST`, `DEPLOY_SSH_USER`, `APP_PORT` печатаются как `***`; открытым остался только `DEPLOY_SSH_PORT: 22` — задокументированное исключение;
  - фильтр дампа журнала прогнан по **настоящему** выводу `systemctl status` и `journalctl` с прод-сервера: греп по запрещённым токенам на выходе пуст;
  - перезапущенный сервис подхватил `SARAFAN_GATEWAY_URL` из env-файла (длина совпадает с исходным URL) — автоимпорт продолжит работать на прогоне в 06:40;
  - прод отвечает 200 на `/`, `/news`, `/admin` до и после деплоя.
- **Попутно укреплён `apply-migration.yml`:** пустой `DATABASE_URI` больше не проходит молча (пайплайн возвращал успех от `sed`, а libpq ушёл бы на локальный сокет и БД по имени пользователя), `psql` в preflight вынесен из `echo "$(…)"` — иначе его код возврата терялся; имя миграции приезжает через `env:` и проверяется по форме.
- **Побочно закрыт silent-failure:** фолбэк `NEXT_PUBLIC_SERVER_URL` на loopback убран — такая сборка уехала бы на прод молча-битой; теперь отдельный шаг падает с `::error::`.
- **Осознанный остаток — 7 позиций в двух systemd-юнитах** (`deploy/kalinino.service`, `deploy/kalinino-vk-sync.service`): имя системного пользователя, рабочий каталог, путь env-файла, зависимость от локального `postgresql.service`, лимит памяти. Убрать без правки самой площадки нельзя: юнит копируется на сервер как есть, плейсхолдер разошёлся бы с установленным. Настоящее лечение — выделенный сервисный пользователь и корень вида `/opt/...`, но это смена раскладки прода, а владелец решил площадку не трогать.
- **Правило поймало ошибку через минуту после написания:** `gh variable set … --body /etc/…` записал `C:/Program Files/Git/etc/…` — Git Bash переписывает аргумент, похожий на unix-путь (MSYS path conversion). Это **пятый слой** к четырём, названным Мозгом, и он не про экранирование: символа-триггера в строке нет вообще. Дописано в канон и в письмо.

## Следующий шаг

1. **Ждать ДК и Мозг.** Когда придёт строка «раздел живёт, редирект стоит, контент сверен» и подтверждение Мозга — план выключения пишет ДК; наша часть: остановить `kalinino` и `kalinino-vk-sync.timer`, снять nginx-сайт и сертификат `kalinino-sdk`, поставить редирект домена на раздел портала (если ДК не сделает сам), архивировать репозиторий.
2. Если перенос затянется — по запросу ДК снять выгрузку заново: `deploy/handover/handover.sh` (+ `export.sql`) — скопировать на бокс и запустить под деплой-пользователем; каталог назначения захардкожен датой 2026-09-04, для повторного снятия сменить `OUT`. Автоимпорт продолжает добавлять посты.
3. Вопросы ДК по данным — отвечать одной строкой; ключ к сопоставлению везде `vkPostId`.
4. Прежние пункты (автопубликация, G192, фото поста 1044, юниты из D-038) **сняты вместе с проектом** — решать их будет ДК в своём разделе, если сочтёт нужным.

> Правка этого раздела и шапки — это обновление **текущего** состояния, а не переписывание истории: разделы «Сделано» прошлых сессий оставлены как есть, включая упоминания, которые сегодняшнее правило уже не разрешило бы (D-038 прямо запрещает их править).

## Открытые вопросы владельцу

- Consent-баннер перед счётчиком — **решено 22.08: не нужен**, достаточно страницы `/privacy` и ссылки в подвале.
- Рубрика «Афиша» — **решено 22.08: завести**, сделано.
- Доливка фото поста 1044 и редактура импортированного контента — на усмотрение владельца.
- `paths-ignore` в `deploy-prod.yml` не отработал при PR #4 — вопрос открыт (класс #104).
- **Реквизиты площадки в systemd-юнитах (из D-038):** убрать их из публичного репозитория можно только сменой раскладки прода — выделенный сервисный пользователь и нейтральный корень вида `/opt/…`. Пока решение владельца — площадку не трогать, поэтому остаток зафиксирован вслух (таблица в `AGENTS.md` §🛡, комментарии в юнитах). Пересмотреть при следующем аудите.
