---
from: KalininoCKS
to: brain
date: 2026-08-04
topic: Импорт контента из ВК завершён: 29 постов, рубрики, медиа, видео — ack мандата 08-03 (ключ в setka.env) и G223/G224
kind: report
compliance: suggest
urgency: normal
---

# Импорт из ВК выполнен (ack мандата 08-03 + отчёт)

## Ack по письмам

- **2026-08-03-your-gateway-key-may-already-be-in-your-env.md (mandate)** — выполнено полностью:
  1. Ключ найден в `/etc/setka/setka.env` на боксе setka: `GATEWAY_KEY_CDK_KALININO` (не `GATEWAY_KEY_KALININO`), активен с 13.07.
  2. Probe со стороны прода: `groups.getById kalinino_sdk` через шлюз → `ok: true, id 218991929, name «МКУК Калининская ЦКС», type: page`. Шлюз отвечает с бокса GONBA (локальная сеть разработчика до шлюза режется).
  3. Секреты не попали в код: ключ лежит только в `/etc/kalinino/kalinino.env` (root-only 600, прод) и локально в `web/.env` (не коммитится). Временные файлы ключей удалены.
- **2026-08-03-g223-payload-draft-false-does-not-publish.md** — соблюдено: публикация идёт через `_status: 'published'` в data (см. `web/src/lib/ingest.ts`, buildPostData), флаг `publish` управляет только правом.

## Что сделано (PR #12, #13, деплой 04.08)

1. Коллекция **Categories** (title/slug/order); `Posts.category` text → relationship; поле `sourceUrl`.
2. **POST /api/ingest/posts**: auth по `SARAFAN_GATEWAY_KEY` (X-Gateway-Key), публикация только с `INGEST_PUBLISH_KEY` (X-Publish-Key) — иначе пост уходит черновиком; идемпотентность по `vkPostId` до перекладывания медиа (G224); опубликованные вручную не перетираются.
3. Импортировано **29 постов** (published=true), все из последних 50 постов ВК: рубрики «Праздники и памятные даты» (12), «Концерты и выступления» (11), «Акции и сборы» (3), «Награды и достижения» (2), «Новости» (1).
4. Медиа: **182 файла** переложены с ВК-CDN в `media` (фотоальбом в постах: cover + gallery), **7 видео** — плеерами ВК (mp4/video_ext.php, воспроизводятся на сайте, лайтбокс умеет оба типа).
5. Фронт: чипы рубрик в ленте `/news`, карточки с обложками, страницы `/news/category/<slug>`, ссылка рубрики в посте. Дата поста = дата оригинала (не «сегодня»).
6. Миграция `20260804_123500` применена на проде до деплоя; досапдейчена колонками `source_url`/`version_source_url` (изначально не вошли — ingest падал 500, пофикшено PR #13).
7. Видео в посты попали по ссылкам на плеер (не скачиваются) — по договорённости с пионером.

## Отклонения/долги

- **2 фото поста #1044 («Питрау» в Нослах)** не загрузились (таймаут ВК-CDN при импорте; 5 из 7 на месте). Повторный ingest опубликованный пост не трогает (by design) — доливка вручную через /admin при желании.
- **16 «пустых» постов** (1 фото без текста — афиши прошедших событий) и **дубли опросов** пропущены осознанно.
- **Drizzle-снапшот (G192)** — отложен снова: локальный Postgres на машине разработчика не поднимается (инстанс падает 0xC0000142 при выходе из шелла, системный 5433 — пароль неизвестен). Миграции пишем руками (.ts + зеркальный .sql).

## Проверка на проде

`/news` 200, `/news/category/prazdniki` 200, пост с видео 200 (видео-плеер встроен), 29 published постов, 5 категорий, 182 медиа-записи. ISR 60 c.

## Предложения

- Долить 2 фото поста 1044 и отредактировать рубрики руками через /admin — при желании владельца.
- Следующий этап наполнения — «свежие» посты по мере выхода (скрипты готовы: `scripts/vk-fetch.mjs` + `scripts/vk-import.mjs`).
