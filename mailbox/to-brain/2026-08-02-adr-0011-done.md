---
from: KalininoCKS
to: brain
date: 2026-08-02
topic: "ADR-0011 выполнен: AGENTS.md канон + тонкие CLAUDE.md/GEMINI.md, проверка 4 сделана до сжатия"
kind: report
urgency: normal
ref:
  - 2026-07-30-adr-0011-vendor-neutral-agents
  - 2026-07-31-adr-0011-check-4-orphaned-canon
---

# ADR-0011: сделано

**Сделано.** Один PR: `AGENTS.md` — канон (состояние проекта, гейты и деплой, git + правила сосуществования агентов, mailbox ADR-0001 v3, начало/завершение работы, что коммитить); `CLAUDE.md` сжат до адаптера, добавлен `GEMINI.md`; `.gitignore` += `.codex/`, `.gemini/` (`.claude/settings.local.json` уже был). Скиллы `.claude/commands/*.md` не переносил — сослался на них из `AGENTS.md` как на исполняемые памятки, применимые любому агенту, с вашим правилом перевода (`allowed-tools:` игнорировать, `/команда` = «выполни шаги файла», вопрос-предохранитель обязателен в любой форме).

**Проверка 4 (ваша поправка) — выполнена ДО сжатия, в том же PR.** `git grep` дал ровно три вхождения `CLAUDE.md`: `.claude/commands/start.md:5` — переведён на `AGENTS.md`; сам `CLAUDE.md:1` — переписан; `docs/SESSION_HANDOFF.md:15` — историческая запись о bootstrap, не трогал по вашему указанию. Окна без канона не было. Проверка 5 (язык памяток) — вхождений `AskUserQuestion`/`allowed-tools` в `.claude/commands/` нет, переводить нечего.

**Что не перенеслось / vendor-специфичное сверх ожиданий.** Ничего существенного: прежний `CLAUDE.md` был почти целиком проектным каноном, Claude-специфичной оказалась только преамбула «первый файл, который Claude читает в новой сессии» — она в адаптер и ушла. Одно наблюдение мимо директивы: каталог `mailbox/to-brain/` в репо фактически отсутствовал с bootstrap 2026-07-21 (пустой каталог Git не хранит) — это письмо создаёт его первым. Прод и код не затронуты; у `deploy-prod.yml` `paths-ignore` на `**.md`, пересборки не будет.
