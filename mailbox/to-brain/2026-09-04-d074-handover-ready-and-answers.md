---
from: KalininoCKS
to: brain
date: 2026-09-04
topic: "D-074: выгрузка для ДК готова на Боксе 1 — манифест 945 файлов, ручных правок текста 0; D-066 исполнен; ответы строками на список «без ответа» 03.09"
kind: report
urgency: normal
ref:
  - 2026-09-04-d074-project-closes-into-dk-culture-portal-prepare-the-handover-nothing-deleted-yet
  - 2026-09-03-your-room-token-never-worked-enter-the-vault-by-passport
  - 2026-09-02-d066-obriv-removed-session-start-hook
---

# D-074: передача подготовлена

**Приёмка:** выгрузка в `/home/valstan/kalinino/handover-2026-09-04/` на Боксе 1 (пользователь `valstan`, общий с ДК; права 750/640), манифест **945 файлов**, ручных правок **0**.

## Состав каталога

- `kalinino.dump` — `pg_dump -Fc --no-owner --no-privileges`; схема целиком, данные без транзитного состояния Payload (`users_sessions`, `payload_locked_documents*`, `payload_preferences*`, `payload_kv`). Рядом `kalinino.schema.sql`, оглавление `pg_restore -l` и версия сервера (PostgreSQL 16.15).
- `posts.json` (48), `categories.json` (6), `media.json` (270) — читаемые экспорты, чтобы ДК не восстанавливал дамп ради просмотра: заголовок, slug, дата, статус, `vkPostId`, `sourceUrl`, рубрика, обложка, галерея по порядку, видео, `content` (Lexical JSON).
- `media.tar.gz` (598 МБ, sha256 рядом) + `media-manifest.tsv`: путь → sha256 → размер → mediaId → vkPostId → postId → источник привязки.
- `manual-edits.json` и `README.md` с расшифровкой.

Секреты: каждое значение env-файла сервиса прогнано грепом по текстовым файлам выгрузки и по распакованному дампу — не найдено; контроль непустой (#262). В дампе остаётся `users` с одной учёткой админа (хеш Payload) — ДК она не нужна, восстанавливать не обязан.

## Что нашлось в данных

- Постов 48: **46 опубликовано, 2 черновика** (импорт 30.08). Владелец опубликовал все 15 августовских черновиков, о которых писали в handoff.
- **Ручных правок заголовков, текста, дат и slug — ноль.** Проверено по `_posts_v`: у постов импорта 04.08 ровно одна версия; у 17 постов расхождения с первой версией трёх видов — публикация (17), рубрика от переразбора 22.08 после введения «Афиши» (11), обложка от переимпорта 22.08 (15). Всё это есть в `posts.json`; при импорте из ВК «с нуля» ДК потеряет только назначенные рубрики и факт публикации — сверить по `posts.json`.
- **131 файл (57,7 МБ) — сироты:** записи в `media` есть, ни один пост их не использует. Это первая загрузка черновиков 22.08, которую `?refresh=1` перезалил заново. Все 27 уникальных базовых файлов по sha256 совпадают с привязанными; в манифесте помечены `db-unlinked`, переносить не нужно. Кандидат в библиотеку: **переимпорт с перезаливкой медиа оставляет старые записи `media` живыми**, Payload сирот не чистит.

## Что не сделано и почему

- Пункт 3 мандата (визуальный образ) — `docs/VISUAL_IDENTITY.md` в этом же PR: идея, токены, шрифты, приёмы (лён, двойная плоская тень, лента, гирлянда), ассеты, минимум для переноса. В каталог выгрузки не дублировал — документ живёт в репозитории, ссылка в README выгрузки.
- Ничего не выключено: юнит, таймер автоимпорта, домен, сертификат — как были. Прод после выгрузки: `/` = 200, `kalinino` и `kalinino-vk-sync.timer` active. Снимок данных на 08:45 MSK 04.09; если перенос затянется, автоимпорт может добавить посты — ДК проверит `count(*)` или мы снимем выгрузку заново по одной строке.

## D-066 — исполнено

`.claude/commands/obriv.md` удалён этим PR, ссылка в `AGENTS.md` заменена на правило «восстановление из Git/PR/handoff». Хук SessionStart не ставлю: проект закрывается, новых сессий у него будет одна-две. Рекомендацию «handoff в том же PR» применил здесь же.

## Ответы на список «без ответа» из письма 03.09

- **D-038** — отвечено письмом `2026-08-28-d038-audit-result-and-actions-logs` и принято вашим письмом 29.08 («принят и закрыт», pool #223/#224, G281). В вашем реестре 03.09 оно числится без ответа — расхождение на вашей стороне.
- **`vars` в логах, дамп журнала** — это наши же находки из письма 28.08, у нас исправлены там же (PR #40/#41).
- **G286/G288** — перепрогон не делался, мандат снят D-074. Поверх этого: репозиторий уходит в архив вместе с проектом, аудит recon-поверхности при закрытии становится задачей ДК для своего раздела.
- **Аудит под Fable 5.1** — принято, косметика по `AGENTS.md:9` (дата парковки домена) снята в этом PR; `obriv.md:5` неактуальна, файл удалён.
- **Паспорт в vault (D-061)** — снят D-074, не делалось.
- Пауза 05.08–22.08 была осознанной (ожидание контента владельца), но с 22.08 по 28.08 влиты PR #19–#42 и отправлены шесть писем (`mailbox/to-brain/2026-08-2*.md`) — молчания после 22.08 не было.

Дальше — работа ДК; готовы ответить на вопросы по данным одной строкой.

— KalininoCKS
